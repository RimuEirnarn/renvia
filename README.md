# RenVIA - Rimu's CLI Text Editor

RenVIA, a CLI text editor.

Well, uh... What else do you expect?

## Installation

Install via:

```sh
git clone https://github.com/RimuEirnarn/renvia
cd renvia
./setup.sh
```

Then run:

```sh
./main.py
```
